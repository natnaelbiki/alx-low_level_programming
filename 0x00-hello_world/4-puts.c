#include<stdio.h>
#include<string.h>

/**
 *  * main - Entry point
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	char text[60];	
	strcpy(text, "\"Programming is like building a multilingual puzzle");
	puts(text);
	return (0);
}
